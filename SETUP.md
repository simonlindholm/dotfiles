## Setup

```
sudo apt install libterm-readkey-perl python3-pip vim git g++ clang htop
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
python3 -m pip install --user git-revise
cargo install git-absorb
```
